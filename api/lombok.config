# If using Lombok with static analysis tools, it might get buggy.

# Stop lombok to search beyond this point
config.stopBubbling=true
# Add @Generated for generated stuffs, static analysis tool might use this. But still might cause issues.
lombok.addLombokGeneratedAnnotation=true